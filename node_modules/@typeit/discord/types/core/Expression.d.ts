import { RuleBuilder, CommandMessage, TypeOrPromise, Client } from "../..";
export declare type Expression = string | RegExp | RuleBuilder;
export declare type ExpressionFunction = (command?: CommandMessage, client?: Client) => TypeOrPromise<Expression>;
