import { Expression, CommandMessage, Client, TypeOrPromise } from "../..";
export declare type ArgsRules<Type extends Expression = Expression> = Type[];
export declare type ArgsRulesFunction<Type extends Expression = Expression> = (command?: CommandMessage, client?: Client) => TypeOrPromise<Type[]>;
