import { Message } from "discord.js";
import { CommandInfos, Expression, ExpressionFunction, ArgsRulesFunction, InfosType, DiscordInfos, DCommand, RuleBuilder } from "../..";
export declare class CommandMessage<ArgsType = any, InfoType extends InfosType = any> extends Message implements CommandInfos<InfoType, Expression> {
    prefix: Expression | ExpressionFunction;
    commandName: Expression | ExpressionFunction;
    description: string;
    infos: InfoType;
    argsRules: ArgsRulesFunction<Expression>[];
    discord: DiscordInfos;
    args: ArgsType;
    commandContent: string;
    static create<InfoType extends InfosType = any>(message: Message, command: DCommand): CommandMessage<any, InfoType>;
    static parseArgs(expression: RuleBuilder[], message: CommandMessage): void;
}
