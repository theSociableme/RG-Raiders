import { Decorator } from "./Decorator";
import { GuardFunction } from "../..";
export declare class DGuard extends Decorator {
    protected _fn: GuardFunction;
    get fn(): GuardFunction<any, any, any>;
    static createGuard(fn: GuardFunction): DGuard;
}
