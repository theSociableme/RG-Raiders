import { RequestHandler, RPCConnection, RPCRequest, RPCResponse } from 'rpc-utils';
import IdentityWallet from './identity-wallet';
declare type Origin = string | null | undefined;
export declare class DidProvider implements RPCConnection {
    protected _handle: RequestHandler;
    protected _wallet: IdentityWallet;
    constructor(wallet: IdentityWallet);
    get isDidProvider(): boolean;
    send(msg: RPCRequest, origin?: Origin): Promise<RPCResponse | null>;
}
export {};
