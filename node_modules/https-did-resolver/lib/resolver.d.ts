import { ParsedDID, DIDDocument } from 'did-resolver';
declare global {
    interface Window {
        XMLHttpRequest: any;
    }
}
export default function getResolver(): {
    'https': (did: string, parsed: ParsedDID) => Promise<DIDDocument | null>;
};
